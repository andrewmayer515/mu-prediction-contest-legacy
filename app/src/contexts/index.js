import { createContext } from 'react';

//---------------------------------------------------------------------

export const ResultContext = createContext({});
export const SetResultContext = createContext();
export const RosterContext = createContext();
